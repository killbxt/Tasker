<Window x:Class="TaskManager.Views.AnalyticsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Аналитика" Height="520" Width="720"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource MaterialDesignWindow}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <materialDesign:PackIcon Kind="ChartBar" Width="28" Height="28" Margin="0,0,8,0"/>
            <TextBlock x:Name="ScopeTitle" Text="Аналитика" FontSize="20" FontWeight="Bold"/>
        </StackPanel>

        <UniformGrid Grid.Row="1" Columns="3" Margin="0,12,0,12">
            <Border Background="{DynamicResource MaterialDesignPaper}" CornerRadius="10" Padding="12" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Закрыто всего" Opacity="0.75"/>
                    <TextBlock x:Name="TotalDoneText" FontSize="22" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="{DynamicResource MaterialDesignPaper}" CornerRadius="10" Padding="12" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="За 7 дней" Opacity="0.75"/>
                    <TextBlock x:Name="Done7DaysText" FontSize="22" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="{DynamicResource MaterialDesignPaper}" CornerRadius="10" Padding="12">
                <StackPanel>
                    <TextBlock Text="Среднее время" Opacity="0.75"/>
                    <TextBlock x:Name="AvgLeadTimeText" FontSize="22" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Border Grid.Row="2" Background="{DynamicResource MaterialDesignPaper}" CornerRadius="10" Padding="12">
            <StackPanel>
                <TextBlock Text="Топ исполнителей (закрыто)" FontWeight="Bold" Margin="0,0,0,8"/>
                <ItemsControl x:Name="TopAssigneesList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <DockPanel Margin="0,4">
                                <TextBlock Text="{Binding Name}" DockPanel.Dock="Left"/>
                                <TextBlock Text="{Binding Count}" DockPanel.Dock="Right" FontWeight="Bold"/>
                            </DockPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Закрыть"
                    Click="Close_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>

