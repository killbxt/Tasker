<Application x:Class="TaskManager.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             StartupUri="Views/LoginWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- VS Code-like dark theme -->
                <materialDesign:BundledTheme BaseTheme="Dark"
                                            PrimaryColor="Grey"
                                            SecondaryColor="Blue" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- App-level surfaces -->
            <SolidColorBrush x:Key="AppBackgroundBrush" Color="#FF1E1E1E"/>
            <SolidColorBrush x:Key="AppPanelBrush" Color="#FF252526"/>
            <SolidColorBrush x:Key="AppBorderBrush" Color="#FF3C3C3C"/>

            <Style TargetType="Window" BasedOn="{StaticResource MaterialDesignWindow}">
                <Setter Property="Background" Value="{StaticResource AppBackgroundBrush}"/>
            </Style>

            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                <Setter Property="Height" Value="36"/>
                <Setter Property="Padding" Value="12,6"/>
            </Style>
            <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignFilledTextBox}"/>
            <Style TargetType="PasswordBox" BasedOn="{StaticResource MaterialDesignFilledPasswordBox}"/>

            <Style x:Key="TaskCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource MaterialDesignCardBackground}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Margin" Value="8"/>
                <Setter Property="Padding" Value="12"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="AppPanelStyle" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource AppPanelBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource AppBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="12"/>
                <Setter Property="Padding" Value="20"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>